import{_ as a,o as s,c as i,an as t}from"./chunks/framework.B1gJEg1M.js";const u=JSON.parse('{"title":"Internals","description":"","frontmatter":{},"headers":[],"relativePath":"internals.md","filePath":"internals.md","lastUpdated":null}'),n={name:"internals.md"};function l(o,e,r,d,c,p){return s(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="Internals" tabindex="-1">Internals <a class="header-anchor" href="#Internals" aria-label="Permalink to &quot;Internals {#Internals}&quot;">​</a></h1><p>This page describes the implementation details of kernel fusion, manual memory management via <code>@analyze_lifetimes</code> and automatic memory management via GC heuristics. For docs on how to use these features, see <a href="./perf/kernel_fusion">Kernel Fusion</a> and <a href="./perf/reduce_allocations">Reduce Allocations</a>.</p><h2 id="Broadcast-fusion" tabindex="-1">Broadcast fusion <a class="header-anchor" href="#Broadcast-fusion" aria-label="Permalink to &quot;Broadcast fusion {#Broadcast-fusion}&quot;">​</a></h2><p>We compile nested Julia broadcast expressions on <code>NDArray</code> to a single CUDA kernel instead of launching one kernel per operation.</p><h3 id="Pipeline" tabindex="-1">Pipeline <a class="header-anchor" href="#Pipeline" aria-label="Permalink to &quot;Pipeline {#Pipeline}&quot;">​</a></h3><ul><li><p><strong>Expression:</strong> You write a dotted or <code>@.</code> expression such as <code>y .= @. a * b + c</code>.</p></li><li><p><strong>Broadcast tree:</strong> Julia builds the <code>Broadcasted</code> tree.</p></li><li><p><strong>Fused path:</strong> Flatten the tree, build a kernel with CUDA.jl, and launch it with Legate.</p></li><li><p><strong>Unfused path:</strong> <code>unravel_broadcast_tree</code> recursively unravels the tree and executes each operation one at a time.</p></li></ul><h2 id="Lifetimes-and-GC" tabindex="-1">Lifetimes and GC <a class="header-anchor" href="#Lifetimes-and-GC" aria-label="Permalink to &quot;Lifetimes and GC {#Lifetimes-and-GC}&quot;">​</a></h2><p>Julia&#39;s GC sees an <code>NDArray</code> as a small handle. The actual data is owned by the Legate runtime. This means that to Julia&#39;s GC <code>NDArrays</code> do not create memory pressure and GC is never executed. To avoid out-of-memory errors we created the <code>@analyze_lifetimes</code> macro so users can manually manage the lifetimes of a code block and also manually track device memory to automatically invoke GC.</p><h3 id="Eager-last-use-freeing-with-@analyze_lifetimes" tabindex="-1">Eager last-use freeing with <code>@analyze_lifetimes</code> <a class="header-anchor" href="#Eager-last-use-freeing-with-@analyze_lifetimes" aria-label="Permalink to &quot;Eager last-use freeing with \`@analyze_lifetimes\` {#Eager-last-use-freeing-with-@analyze_lifetimes}&quot;">​</a></h3><p><code>@analyze_lifetimes</code> rewrites a block at macro-expansion time:</p><ul><li><p>Hoist temporary allocations into named temps.</p></li><li><p>Find each temp&#39;s static last use.</p></li><li><p>Insert calls to free temporary <code>NDArrays</code> after last-use.</p></li></ul><p>Under broadcast fusion, intermediate dotted nodes are <strong>not</strong> real <code>NDArray</code> allocations. The macro switches to a fusion-aware hoist that keeps dotted trees lazy and only treats slices, broadcast roots, and non-broadcast calls as real allocations. When fusion is off, every call (including dotted ops) is treated as a real allocation.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@analyze_lifetimes</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> begin</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    result </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> A[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">end</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, :] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> B[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">end</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, :]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    C .</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> result </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span></code></pre></div><p>Use <code>@show_lifetimes</code> to print the rewritten block and the free sites without running the code. That is pure AST work and works without a GPU.</p><p>The implementation uses separate passes for inter-statement broadcast fusion, allocation hoisting, and finalizer insertion. The top-level scoping pass selects the appropriate lifetime analysis based on whether broadcast fusion is enabled.</p><ul><li><p><strong>Inter-statement broadcast fusion</strong> (<code>rewrite_scope</code>): Merges single-use broadcast statements into their consumer, e.g.: <code>p = A .* B; C .= p .+ 1</code> → <code>C .= A .* B .+ 1</code> This pass operates only on syntax and does not depend on cuNumeric types.</p></li><li><p><strong>Lifetime analysis</strong>:</p><ul><li><p>With fusion enabled (<code>rewrite_broadcast_lifetimes</code>): keeps dotted trees lazy and hoists only materialized values (slices, non-broadcast calls, etc.)</p></li><li><p>With fusion disabled (<code>rewrite_eager_lifetimes</code>): hoists all allocating calls</p></li></ul><p>Both passes rename temps and insert free calls after static last uses.</p></li><li><p><strong>Finalizer insertion</strong> (<code>insert_finalizers</code>): Traverses the AST generated by either lifetime pass and inserts <code>delete</code> calls at the computed last-use sites. It preserves the final return value of the block by not freeing it.</p></li></ul><p>Limits to keep in mind:</p><ul><li><p>Analysis is statement-linear. It is not a full control-flow graph pass. Wrap hot loop bodies, not entire programs.</p></li><li><p>Some paths free eagerly outside the macro (for example LHS slice views created during indexed assignment).</p></li></ul><p>Relevant source: <code>src/scoping/</code>.</p><h3 id="Allocation-driven-GC-heuristics" tabindex="-1">Allocation-driven GC heuristics <a class="header-anchor" href="#Allocation-driven-GC-heuristics" aria-label="Permalink to &quot;Allocation-driven GC heuristics {#Allocation-driven-GC-heuristics}&quot;">​</a></h3><p>Every <code>NDArray</code> registers its byte size on construct and free. When predicted live bytes cross soft (~80%) or hard (~90%) fractions of available memory, and enough new growth has accumulated since the last collection, cuNumeric.jl triggers Julia <code>GC.gc</code>.</p><p><code>@analyze_lifetimes</code> reduces peak live temps. The heuristics catch cases the macro cannot see.</p><p>Relevant source: <code>src/memory.jl</code>.</p>`,23)])])}const m=a(n,[["render",l]]);export{u as __pageData,m as default};
