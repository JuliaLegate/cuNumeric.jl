import{_ as o,o as a,c as t,aA as i}from"./chunks/framework.uT7NM1Ik.js";const p=JSON.parse('{"title":"Performance Tips","description":"","frontmatter":{},"headers":[],"relativePath":"perf.md","filePath":"perf.md","lastUpdated":null}'),r={name:"perf.md"};function n(d,e,c,l,s,u){return a(),t("div",null,[...e[0]||(e[0]=[i('<h1 id="Performance-Tips" tabindex="-1">Performance Tips <a class="header-anchor" href="#Performance-Tips" aria-label="Permalink to &quot;Performance Tips {#Performance-Tips}&quot;">​</a></h1><h2 id="Avoid-Scalar-Indexing" tabindex="-1">Avoid Scalar Indexing <a class="header-anchor" href="#Avoid-Scalar-Indexing" aria-label="Permalink to &quot;Avoid Scalar Indexing {#Avoid-Scalar-Indexing}&quot;">​</a></h2><p>Accessing elements of an NDArray one at a time (e.g., <code>arr[5]</code>) is slow and should be avoided. Indexing like this requires data to be trasfered between device and host and maybe even communicated across nodes. Scalar indexing will emit an error which can be opted out of with <code>@allowscalar</code> or <code>allwoscalar() do ... end</code>. Several functions in the existing API invoke scalar indexing and are intended for testing (e.g., the <code>==</code> operator).</p><h2 id="Avoid-Implicit-Promotion" tabindex="-1">Avoid Implicit Promotion <a class="header-anchor" href="#Avoid-Implicit-Promotion" aria-label="Permalink to &quot;Avoid Implicit Promotion {#Avoid-Implicit-Promotion}&quot;">​</a></h2><p>Mixing integral types of different size (e.g., <code>Float64</code> and <code>Float32</code>) will result in implicit promotion of the smaller type to the larger types. This creates a copy of the data and hurts performance. Implicit promotion from a smaller integral type to a larger integral type will emit an error which can be opted out of with <code>@allowpromotion</code> or <code>allowpromotion() do ... end</code>. This error is common when mixing literals with <code>NDArrays</code>. By default a floating point literal (i.e., 1.0) is <code>Float64</code> but the default type of an <code>NDArray</code> is <code>Float32</code>.</p><h2 id="Setting-Hardware-Configuration" tabindex="-1">Setting Hardware Configuration <a class="header-anchor" href="#Setting-Hardware-Configuration" aria-label="Permalink to &quot;Setting Hardware Configuration {#Setting-Hardware-Configuration}&quot;">​</a></h2><p>There is no programatic way to set the hardware configuration used by CuPyNumeric (as of 26.01). By default, the hardware configuration is set automatically by Legate. This configuration can be manipulated through the following environment variables:</p><ul><li><p><code>LEGATE_SHOW_CONFIG</code> : When set to 1, the Legate config is printed to stdout</p></li><li><p><code>LEGATE_AUTO_CONFIG</code>: When set to 1, Legate will automatically choose the hardware configuration</p></li><li><p><code>LEGATE_CONFIG</code>: A string representing the hardware configuration to set</p></li></ul><p>These variables must be set before launching the Julia instance running cuNumeric.jl. We recommend setting <code>export LEGATE_SHOW_CONFIG=1</code> so that the hardware configuration will be printed when Legate starts. This output is automatically captured and relayed to the user.</p><p>To manually set the hardware configuration, <code>export LEGATE_AUTO_CONFIG=0</code>, and then define your own config with something like <code>export LEGATE_CONFIG=&quot;--gpus 1 --cpus 10 --ompthreads 10&quot;</code>. We recommend using the default memory configuration for your machine and only settings the <code>gpus</code>, <code>cpus</code> and <code>ompthreads</code>. More details about the Legate configuration can be found in the <a href="https://docs.nvidia.com/legate/latest/usage.html#resource-allocation" target="_blank" rel="noreferrer">NVIDIA Legate documentation</a>. If you know where Legate is installed on your computer you can also run <code>legate --help</code> for more detailed information.</p><h2 id="Kernel-Fusion" tabindex="-1">Kernel Fusion <a class="header-anchor" href="#Kernel-Fusion" aria-label="Permalink to &quot;Kernel Fusion {#Kernel-Fusion}&quot;">​</a></h2><p>cuPyNumeric does not fuse independent operations automatically, even in broadcast expressions. This is a priority for a future release.</p>',12)])])}const m=o(r,[["render",n]]);export{p as __pageData,m as default};
