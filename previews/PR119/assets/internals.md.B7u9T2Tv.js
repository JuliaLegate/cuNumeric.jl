import{_ as s,o as a,c as i,an as t}from"./chunks/framework.ZoWi1RxU.js";const u=JSON.parse('{"title":"Internals","description":"","frontmatter":{},"headers":[],"relativePath":"internals.md","filePath":"internals.md","lastUpdated":null}'),n={name:"internals.md"};function l(o,e,r,d,c,p){return a(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="Internals" tabindex="-1">Internals <a class="header-anchor" href="#Internals" aria-label="Permalink to &quot;Internals {#Internals}&quot;">​</a></h1><p>This page describes how two performance features work under the hood: broadcast fusion, and the memory management path built around <code>@analyze_lifetimes</code> and GC heuristics. For day-to-day usage tips, see <a href="./perf/kernel_fusion">Kernel Fusion</a> and <a href="./perf/reduce_allocations">Reduce Allocations</a>.</p><h2 id="Broadcast-fusion" tabindex="-1">Broadcast fusion <a class="header-anchor" href="#Broadcast-fusion" aria-label="Permalink to &quot;Broadcast fusion {#Broadcast-fusion}&quot;">​</a></h2><p>Nested Julia broadcast expressions on <code>NDArray</code> can compile to a single CUDA PTX kernel instead of launching one cuNumeric op per node in the expression tree. Fusion is automatic when it is enabled (the default). Users turn it on or off through <code>CNPreferences</code>; see <a href="./configuration">Configuration</a>.</p><h3 id="Pipeline" tabindex="-1">Pipeline <a class="header-anchor" href="#Pipeline" aria-label="Permalink to &quot;Pipeline {#Pipeline}&quot;">​</a></h3><ol start="2"><li><p>You write a dotted or <code>@.</code> expression such as <code>y .= @. a * b + c</code>.</p></li><li><p>Julia builds a lazy <code>Broadcasted</code> tree.</p></li><li><p><code>copyto!(::NDArray, bc)</code> decides whether to fuse or fall back.</p></li><li><p><strong>Fused path:</strong> promotion checks, flatten the tree, split runtime vs static args, build a KernelAbstractions linear kernel, compile or reuse PTX, launch through Legate&#39;s <code>RUN_PTX_BROADCAST</code> task.</p></li><li><p><strong>Unfused path:</strong> <code>unravel_broadcast_tree</code> materializes intermediate <code>NDArray</code>s and calls one C-API ufunc per node.</p></li></ol><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Eligible for fusion (nested ops, same shapes, CUDA available)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">y .</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> @.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> a </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> b </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> c </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">/</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> d</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Typical unfused fallback: shape-mismatched broadcast such as matrix .+ vector</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># (or fusion disabled / below the min-ops threshold)</span></span></code></pre></div><h3 id="Design-points" tabindex="-1">Design points <a class="header-anchor" href="#Design-points" aria-label="Permalink to &quot;Design points {#Design-points}&quot;">​</a></h3><ul><li><p>Fusion lives inside Julia&#39;s broadcast <code>copyto!</code> path. It is not a general compiler pass over arbitrary Julia code.</p></li><li><p>Only <strong>linear, same-shape</strong> array leaves fuse. Mismatched shapes fall back to the unfused path.</p></li><li><p>The min-ops preference (default <code>2</code>) keeps trivial single-op broadcasts like <code>y .= cos.(x)</code> on the mature C-API path, avoiding PTX compile cost. Set it to <code>1</code> via <code>CNPreferences.set_broadcast_fusion_min_ops!(1)</code> if you want those fused too (restart Julia afterward).</p></li><li><p>Preference changes are compile-time. Restart Julia after changing them. The ENV override <code>CUNUMERIC_FUSE_BROADCAST_MIN_OPS</code> is documented on the <a href="./configuration">Configuration</a> page; prefer <code>CNPreferences</code> for normal use.</p></li><li><p>For debugging fused launches and lifetime rewrites, see <a href="./debugging">Debugging</a>.</p></li></ul><p>Relevant source: <code>src/ndarray/broadcast.jl</code>, <code>src/ndarray/broadcast_fusion.jl</code>.</p><h2 id="Lifetimes-and-GC" tabindex="-1">Lifetimes and GC <a class="header-anchor" href="#Lifetimes-and-GC" aria-label="Permalink to &quot;Lifetimes and GC {#Lifetimes-and-GC}&quot;">​</a></h2><p>Julia&#39;s GC sees an <code>NDArray</code> as a small handle. The real cost lives in Legion / CUDA buffers. cuNumeric.jl therefore uses two cooperating layers: eager last-use freeing, and allocation-driven GC heuristics.</p><h3 id="Eager-last-use-freeing-with-@analyze_lifetimes" tabindex="-1">Eager last-use freeing with <code>@analyze_lifetimes</code> <a class="header-anchor" href="#Eager-last-use-freeing-with-@analyze_lifetimes" aria-label="Permalink to &quot;Eager last-use freeing with \`@analyze_lifetimes\` {#Eager-last-use-freeing-with-@analyze_lifetimes}&quot;">​</a></h3><p><code>@analyze_lifetimes</code> rewrites a block at macro-expansion time: 2. Hoist temporary allocations into named temps.</p><ol start="3"><li><p>Find each temp&#39;s static last use.</p></li><li><p>Insert <code>maybe_insert_delete</code> (which calls <code>destroy!</code> for <code>NDArray</code>s) right after that last use.</p></li></ol><p>Under broadcast fusion, intermediate dotted nodes are <strong>not</strong> real <code>NDArray</code> allocations. The macro switches to a fusion-aware hoist that keeps dotted trees lazy and only treats slices, broadcast roots, and non-broadcast calls as real allocations. When fusion is off, every call (including dotted ops) is treated as a real allocation.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@analyze_lifetimes</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> begin</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    result </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> A[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">end</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, :] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> B[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">end</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, :]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    C .</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> result </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span></code></pre></div><p>Use <code>@show_lifetimes</code> to print the rewritten block and the free sites without running the code. That is pure AST work and works without a GPU.</p><p>Limits to keep in mind:</p><ul><li><p>Analysis is statement-linear. It is not a full control-flow graph pass. Wrap hot loop bodies, not entire programs.</p></li><li><p>Some paths free eagerly outside the macro (for example LHS slice views created during indexed assignment).</p></li></ul><p>Relevant source: <code>src/scoping.jl</code>.</p><h3 id="Allocation-driven-GC-heuristics" tabindex="-1">Allocation-driven GC heuristics <a class="header-anchor" href="#Allocation-driven-GC-heuristics" aria-label="Permalink to &quot;Allocation-driven GC heuristics {#Allocation-driven-GC-heuristics}&quot;">​</a></h3><p>Every <code>NDArray</code> registers its byte size on construct and free. When predicted live bytes cross soft (~80%) or hard (~90%) fractions of available Legion memory, and enough new growth has accumulated since the last collection (hysteresis), cuNumeric triggers Julia <code>GC.gc</code>. Afterward it recalibrates counters from Legion&#39;s allocated-byte queries.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>on allocate:</span></span>
<span class="line"><span>  if live bytes &gt; hard limit (~90%): full GC, then recalibrate</span></span>
<span class="line"><span>  else if live bytes &gt; soft limit (~80%): incremental GC, then recalibrate</span></span>
<span class="line"><span>  else: allow the next spike to fire without waiting on the post-GC floor</span></span></code></pre></div><p><code>@analyze_lifetimes</code> reduces peak live temps. The heuristics catch cases the macro cannot see. You can disable the heuristics with <code>cuNumeric.disable_gc!()</code> (used in some tests).</p><p>Relevant source: <code>src/memory.jl</code>.</p>`,26)])])}const k=s(n,[["render",l]]);export{u as __pageData,k as default};
