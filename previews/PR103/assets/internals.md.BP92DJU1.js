import{_ as a,o as t,c as s,an as n}from"./chunks/framework.4paM3Swh.js";const h=JSON.parse('{"title":"Internals","description":"","frontmatter":{},"headers":[],"relativePath":"internals.md","filePath":"internals.md","lastUpdated":null}'),o={name:"internals.md"};function i(r,e,l,d,c,u){return t(),s("div",null,[...e[0]||(e[0]=[n('<h1 id="Internals" tabindex="-1">Internals <a class="header-anchor" href="#Internals" aria-label="Permalink to &quot;Internals {#Internals}&quot;">​</a></h1><p>This page summarizes the machinery behind <a href="./perf/reduce_allocations"><code>@accelerate</code></a>, <a href="./perf/kernel_fusion">kernel fusion</a>, and allocation-driven garbage collection.</p><h2 id="Broadcast-fusion" tabindex="-1">Broadcast fusion <a class="header-anchor" href="#Broadcast-fusion" aria-label="Permalink to &quot;Broadcast fusion {#Broadcast-fusion}&quot;">​</a></h2><p>Julia first builds a nested <code>Broadcasted</code> tree for a dotted expression. When CUDA fusion is enabled and the tree is eligible, cuNumeric flattens it, generates a CUDA.jl kernel, and launches it through Legate. Otherwise <code>unravel_broadcast_tree</code> evaluates the operations individually. CPU execution always uses the unfused path.</p><p><code>@accelerate</code> adds an inter-statement syntax pass. It can merge a single-use broadcast producer into its consumer when doing so preserves mutation and aliasing semantics. The soft-scope <code>begin</code> form instead keeps named values materialized and may lower an eligible chain to a multi-output CUDA kernel.</p><p>Relevant source: <code>src/ndarray/broadcast_fusion.jl</code> and <code>src/scoping/</code>.</p><h2 id="Lifetime-analysis" tabindex="-1">Lifetime analysis <a class="header-anchor" href="#Lifetime-analysis" aria-label="Permalink to &quot;Lifetime analysis {#Lifetime-analysis}&quot;">​</a></h2><p>An <code>NDArray</code> is a small Julia handle to storage owned by Legate, so Julia&#39;s heap pressure understates the size of live array data. <code>@accelerate</code> therefore performs static last-use analysis: 2. Expand nested <code>@.</code> macros and reject non-straight-line code.</p><ol start="3"><li><p>Apply conservative inter-statement fusion where the selected macro form permits it.</p></li><li><p>Hoist materialized temporary values and find their final uses.</p></li><li><p>Insert <code>maybe_insert_delete</code> calls after those uses while protecting caller-owned arguments, named soft-scope bindings, and returned values.</p></li></ol><p>With fusion enabled, nested dotted nodes remain lazy and are not counted as separate array allocations. With fusion disabled, allocating calls are analyzed individually. <code>@show_lifetimes</code> prints the exact expansion without running it.</p><p>The analysis is statement-linear rather than a control-flow graph pass. Apply it to straight-line function or loop bodies, not to control flow itself.</p><h2 id="Allocation-driven-GC" tabindex="-1">Allocation-driven GC <a class="header-anchor" href="#Allocation-driven-GC" aria-label="Permalink to &quot;Allocation-driven GC {#Allocation-driven-GC}&quot;">​</a></h2><p>Each <code>NDArray</code> reports its byte size when constructed and freed. When predicted live bytes cross soft and hard fractions of available memory—and enough new growth has accumulated—cuNumeric asks Julia to collect garbage. Eager last-use freeing reduces peak live storage; the GC heuristic covers allocations the static pass cannot prove dead.</p><p>Relevant source: <code>src/memory.jl</code>.</p>',14)])])}const f=a(o,[["render",i]]);export{h as __pageData,f as default};
